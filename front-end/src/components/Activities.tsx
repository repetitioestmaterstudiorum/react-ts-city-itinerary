import React from "react";

const Activities: React.FC = () => {
  // const fetchActivities = () => {
  //   axios
  //     .get(`http://localhost:${port}/activities/${activity}`)
  //     .then(res => {
  //       setCityItineraries(res.data);
  //     });
  // };

  return <div></div>;
};

export default Activities;
